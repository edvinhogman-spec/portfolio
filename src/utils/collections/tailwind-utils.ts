/**
 * Utility to provide autocompletion for writing tailwind classes outside of JSX elements.
 */
function className(className: string) {
    return className
}

export const tailwindUtils = {
    className,
}
